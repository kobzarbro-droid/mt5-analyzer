# MT5 Portfolio Analyzer 🤖

AI-система анализа портфелей для торговых стратегий MetaTrader 5 (MT5). Это приложение использует GPT-4o от OpenAI для предоставления комплексного анализа, рекомендаций и выводов по торговым портфелям.

## Функции

- **AI Анализ Портфеля**: Использование GPT-4o для анализа до 5 торговых стратегий
- **Комплексные Метрики**: Отслеживание Equity, Drawdown, Correlation, Recovery Factor и Profit
- **Умные Рекомендации**: Получение практических советов по оптимизации стратегий
- **Оценка Рисков**: Определение сильных сторон, слабостей и рисковой экспозиции
- **Интерактивный UI**: Современный веб-интерфейс с отображением результатов в модальном окне
- **Экспорт Данных**: Экспорт результатов анализа в формате JSON для дальнейшей обработки

## Архитектура

Приложение состоит из двух основных компонентов:

1. **Backend (Python/Flask)**: REST API для интеграции с OpenAI и анализа портфеля
2. **Frontend (HTML/CSS/JavaScript)**: Интерактивный веб-интерфейс для ввода данных и визуализации

## Установка

### Требования

- Python 3.8+
- API ключ OpenAI (получить на [OpenAI Platform](https://platform.openai.com/))

### Шаги установки

1. **Клонировать репозиторий**
   ```bash
   git clone https://github.com/kobzarbro-droid/mt5-analyzer.git
   cd mt5-analyzer
   ```

2. **Установить зависимости Python**
   ```bash
   pip install -r requirements.txt
   ```

3. **Настроить переменные окружения**
   ```bash
   cp .env.example .env
   ```
   
   Отредактировать `.env` и добавить ваш API ключ OpenAI:
   ```
   OPENAI_API_KEY=ваш_настоящий_api_ключ
   ```

4. **Запустить backend сервер**
   ```bash
   cd backend
   python api.py
   ```
   
   API будет доступен по адресу `http://localhost:5000`

5. **Открыть frontend**
   
   Откройте `frontend/index.html` в веб-браузере, или запустите простой HTTP сервер:
   ```bash
   cd frontend
   python -m http.server 8000
   ```
   
   Затем перейдите по адресу `http://localhost:8000`

## Использование

### Основной рабочий процесс

1. **Добавить стратегии**: Нажмите "Add Strategy" для добавления до 5 торговых стратегий
2. **Ввести метрики**: Заполните метрики для каждой стратегии:
   - **Name**: Идентификатор стратегии
   - **Equity**: Текущее значение капитала
   - **Drawdown**: Максимальная просадка в процентах
   - **Correlation**: Коэффициент корреляции с другими стратегиями
   - **Recovery Factor**: Коэффициент восстановления
   - **Profit**: Общая прибыль в процентах

3. **Анализировать**: Нажмите "Analyze Portfolio" для отправки данных в AI
4. **Просмотреть результаты**: Анализ появится в модальном окне с:
   - Общей оценкой портфеля
   - Рекомендациями по стратегиям
   - Сильными и слабыми сторонами
   - Анализом рисков
   - Практическими рекомендациями

5. **Экспорт**: Сохраните анализ как JSON для будущего использования

### Тестовые данные

Нажмите "Load Sample Data" для заполнения формы примерами стратегий для тестирования.

## Тестовый режим

Для тестирования без реального API ключа OpenAI:

1. Запустите тестовый backend:
   ```bash
   cd backend
   PORT=5001 python api_test_mock.py
   ```

2. Откройте `frontend/index_test.html` в браузере

Тестовый режим использует mock AI ответы для демонстрации функционала.

## API Документация

### Эндпоинты

#### `GET /health`
Проверка состояния сервиса.

**Ответ:**
```json
{
  "status": "healthy",
  "service": "MT5 Portfolio Analyzer"
}
```

#### `POST /api/portfolio/analyze`
Анализ портфеля стратегий.

**Тело запроса:**
```json
{
  "strategies": [
    {
      "name": "Стратегия 1",
      "equity": 150000,
      "drawdown": -15.5,
      "correlation": 0.65,
      "recovery": 2.3,
      "profit": 45.2
    }
  ]
}
```

**Ответ:**
```json
{
  "success": true,
  "full_analysis": "Детальный AI анализ...",
  "strategies_count": 5,
  "model": "gpt-4o"
}
```

## Структура проекта

```
mt5-analyzer/
├── backend/
│   ├── __init__.py
│   ├── api.py                    # Flask REST API
│   ├── api_test_mock.py         # Тестовый API с mock ответами
│   ├── portfolio_analyzer.py    # Основная логика анализа
│   └── test_api.py              # Юнит-тесты
├── frontend/
│   ├── index.html               # Основной UI
│   ├── index_test.html          # Тестовый UI
│   └── static/
│       ├── app.js               # Frontend JavaScript
│       ├── app_test.js          # Тестовый JavaScript
│       └── styles.css           # UI стили
├── requirements.txt             # Python зависимости
├── .env.example                # Шаблон окружения
├── .gitignore                  # Правила git ignore
├── README.md                   # Документация (EN)
└── README_RU.md               # Документация (RU)
```

## Настройка

### Переменные окружения

- `OPENAI_API_KEY`: Ваш API ключ OpenAI (обязательно)
- `PORT`: Порт backend сервера (по умолчанию: 5000)
- `DEBUG`: Включить режим отладки (по умолчанию: False)

## Безопасность

⚠️ **Важные соображения безопасности:**

1. Никогда не коммитьте файл `.env` и не раскрывайте ваш API ключ OpenAI
2. Рассмотрите внедрение ограничения частоты запросов для продакшена
3. Валидируйте и санитизируйте все пользовательские входные данные
4. Используйте HTTPS в продакшен окружении
5. Внедрите аутентификацию для продакшен развертываний

## Вклад в проект

Вклады приветствуются! Пожалуйста, следуйте этим шагам:

1. Форкните репозиторий
2. Создайте ветку функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## Лицензия

Этот проект является открытым исходным кодом и доступен по лицензии MIT.

## Устранение неполадок

### Распространенные проблемы

**Проблема**: "OpenAI API key not provided"
- **Решение**: Убедитесь, что файл `.env` существует с валидным `OPENAI_API_KEY`

**Проблема**: Ошибки CORS в браузере
- **Решение**: Убедитесь, что Flask-CORS установлен и правильно настроен

**Проблема**: Connection refused при API вызовах
- **Решение**: Проверьте, что backend сервер запущен на правильном порту

## Дорожная карта

- [ ] Добавить аутентификацию пользователей
- [ ] Внедрить отслеживание исторического анализа
- [ ] Добавить больше визуализационных графиков
- [ ] Поддержка дополнительных метрик
- [ ] Мультиязычная поддержка
- [ ] Улучшения для мобильных устройств

## Контакты

По вопросам или поддержке, пожалуйста, откройте issue на GitHub.

---

**Создано с ❤️ для MT5 трейдеров**
